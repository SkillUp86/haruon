<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haruon.groupware.user.mapper.EmprofileMapper">
	<!-- 사진 저장 -->
	<insert id="saveProfileByUser" parameterType="com.haruon.groupware.user.entity.EmpFile">
		insert into emp_file (
				emp_no
			,	original_name
			,	file_name
			,	ext
			,	kind
			,	size
		) values (
				#{empNo},
				#{originalName},
				#{fileName},
				#{ext},
				#{kind},
				#{size}
		)
	</insert>
	<!-- 사진 불러오기 -->
	<select id="findEmpFileByUser" parameterType="Integer" resultType="com.haruon.groupware.user.entity.EmpFile">
		select
				emp_no empNo
			,	original_name originalName
			,	file_name filaName
			,	ext
			,	kind
			,	size
		from emp_file
		where emp_no = #{emp_no}
	</select>
</mapper>