<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.haruon.groupware.franchise.mapper.FranchiseMapper">
	
	<!-- 가맹점 추가 -->
	<insert id="insertFranchise" parameterType="com.haruon.groupware.franchise.entity.Franchise">
		INSERT INTO franchise (
			fname
			, address
			, post_code
			, id
			, leader
			, phone
			, email
		) VALUES (
			#{fname}, #{address}, #{postCode}, #{id}, #{leader}, #{phone}, #{email}
		)
	</insert>
	
	<!-- 가맹점 수 -->
	<select id="selectFranchisesCount" resultType="Integer">
		SELECT count(*)
		FROM franchise
	</select>	
	
	<!-- 가맹점 리스트 -->
	<select id="selectFranchiseList" resultType="com.haruon.groupware.franchise.entity.Franchise">
		SELECT
			fra_no fraNo
			, fname
			, post_code postCode
			, address
			, id
			, leader
			, phone
			, email
			, open_yn openYn
			, create_date createDate
			, update_date updateDate
		FROM franchise
	</select>
	
</mapper>